node_modules/
data/
.env
*.log
*.zip

# Agent / editor scratch
.claude/

# Local dev helpers (not part of the app)
scripts/preview-app.mjs
scripts/preview-static.cjs

# Runtime + build artifacts and staging snapshots (not source)
uploaded-assets/
artifacts/
.zip_stage_*/

# web/dist is 100% REGENERATED by `npm run build:web` (rm dist + copy public->dist) on every Render
# deploy — committing it was ~1.4GB of dead weight Render overwrites anyway. Never commit it.
web/dist/
# PFP intake STAGING — raw Codex drops that pfp-intake.mjs processes into the real folders. Left on disk
# for resume, never served → ~743MB of pure git bloat. Local-only.
web/public/pfp/_incoming/
# Codex image-bridge generation logs (scratch)
_codexlog_*.txt
# R2 upload manifest (local bookkeeping for the CDN sync)
.pfp-r2-manifest.json

# Video recorder toolchain + outputs (local only)
tools/
trailer-*.png
.playwright-mcp/
media-out/


# raw 3D model sources (too big to commit)
/asset-source/

# autopilot secrets + scratch artifacts (do not commit)
OWNER-KEY.txt
test-card.cjs
*-results.txt
_bgtmp/
card-*.png
preview-*.png
demo-*.mp4
slime-green-preview.png
concept*.png
v2-*.png
v3-*.png
ogre*.png
vid-*.mp4

# local dev preview config
.claude/launch.json

# LibreOffice lock files
.~lock.*#
